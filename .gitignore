# Python
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Test / coverage artifacts
.coverage
.coverage.*
htmlcov/
.pytest_cache/
.ruff_cache/

# Virtual envs
.venv

# Project data — never commit personal health data or generated briefings
data/
briefings/
logs/
reports/

# Local secrets / env-specific config (Garmin creds, paths, host overrides)
.env
.env.local

# OS / editor cruft
.DS_Store
.idea/
.vscode/
*.swp

# Claude Code local runtime state
.claude/

# one-off image strays (e.g. LinkedIn banners) must never land in the repo
/*.png
